class Tune {
    /**
     * Tune data
     * 
     * @param { String } name - Name of this tune.
     * @param { String } data - Sound data.
     */
    constructor(name, data) {
        this.name = name;
        this.data = data;
    }
}